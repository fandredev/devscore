import styled from "styled-components"
import { fullCenter } from "../../styles/functions"

export const Container = styled.main`
  padding: 0 2rem;
  ${fullCenter};
  text-align: center;
`

export const SectionInvite = styled.section`
  margin-top: 3rem;
`
